<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <include
            resource="org/springframework/boot/logging/logback/defaults.xml" />

    <springProperty scope="context" name="springAppName" source="application.instance.name"/>

    <!--	<property name="CONSOLE_LOG_PATTERN"-->
    <!--			  value="%d{yyyy-MM-dd HH:mm:ss.SSS}%d{Z}|severity=%level|pid=${PID:-}|service=${application_name}|trace=%X{X-B3-TraceId}|span=%X{X-B3-SpanId}|thread=%t|class=%c{40}|message=%replace(%msg){'\n', '\t'}%n" />-->

    <property name="CONSOLE_LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS}%d{Z}|severity=%level|pid=${PID:-}|thread=%t|class=%c{10}|message=%replace(%msg){'\n', '\t'}%n" />

    <appender name="jsonConsoleAppender"
              class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </layout>
    </appender>

    <appender name="FILE-ROLLING" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>./logs/app.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>./logs/app.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- each archived file, size max 10MB -->
            <maxFileSize>10MB</maxFileSize>
            <!-- total size of all archive files, if total size > 20GB, it will delete old archived file -->
            <totalSizeCap>2GB</totalSizeCap>
            <!-- 60 days to keep -->
            <maxHistory>60</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%d %p %c{1.} [%t] %m%n</pattern>
        </encoder>
    </appender>

    <appender name="Async-Console-Appender"
              class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>512</queueSize>
        <appender-ref ref="jsonConsoleAppender" />
    </appender>

    <logger name="org.springframework" level="WARN"
            additivity="false">
        <appender-ref ref="Async-Console-Appender" />
    </logger>

    <logger name="org.springframework.web" level="TRACE" additivity="false">
        <appender-ref ref="Async-Console-Appender" />
    </logger>

    <logger name="logging.level.org.hibernate.SQL" level="TRACE" additivity="false">
        <appender-ref ref="Async-Console-Appender" />
    </logger>

<!--    <logger name="org.apache" level="TRACE"	additivity="false">-->
<!--        <appender-ref ref="Async-Console-Appender" />-->
<!--    </logger>-->

    <logger name="am.neovision.admin.toolkit" level="debug" additivity="false">
        <appender-ref ref="Async-Console-Appender" />
    </logger>

    <root level="INFO">
        <appender-ref ref="Async-Console-Appender" />
    </root>


</configuration>